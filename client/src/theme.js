export const lightTheme = {
  textColor: 'black',
  bgColor: 'white',
};

export const darkTheme = {
  textColor: 'white',
  bgColor: 'black',
};

export const theme = {
  lightTheme,
  darkTheme,
};

export default theme;
